{
  "name": "astriarch",
  "version": "1.0.0",
  "description": "Astriarch space strategy game monorepo",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "nx format",
    "build": "nx run-many --target=build --all"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nx": "^15.0.13"
  }
}
